{
  "name": "@cig/chatbot",
  "version": "0.1.92",
  "description": "CIG conversational interface with RAG pipeline",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@langchain/openai": "^0.1.0",
    "chromadb": "^1.8.1",
    "langchain": "^0.2.0"
  },
  "devDependencies": {
    "fast-check": "^3.23.2",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
