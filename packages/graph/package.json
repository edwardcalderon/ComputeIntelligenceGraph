{
  "name": "@cig/graph",
  "version": "0.1.79",
  "description": "CIG Neo4j graph engine",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "neo4j-driver": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "fast-check": "^3.23.2",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
