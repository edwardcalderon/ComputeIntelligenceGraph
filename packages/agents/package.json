{
  "name": "@cig/agents",
  "version": "0.1.99",
  "description": "CIG agents: OpenClaw (query reasoning) and OpenFang (action execution)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@langchain/openai": "^0.1.0",
    "@langchain/core": "^0.2.0",
    "langchain": "^0.2.0",
    "@cig/chatbot": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^3.23.2",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
